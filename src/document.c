#include <stdlib.h>
#include "document.h"



document_t *document_create()
{
    // document_t *document = malloc(sizeof(document));
    // return document;
    return NULL;
}

void document_destroy(document_t *document)
{
    // list_destroy(document->words);
    // map_destroy(document->name, NULL, NULL);
    // free(document);
}